import styles from "../title/styles.module.css";

const Title = () => {
  return (
    <div className={styles.container}>
      <p className={styles.container__title}>The Question</p>
    </div>
  );
};

export default Title;
